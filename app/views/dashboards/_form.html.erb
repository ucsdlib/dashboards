<%= form_with(model: dashboard, local: true) do |form| %>
  <% if dashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

      <ul>
      <% dashboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rdd_attribute %>
    <%= form.text_field :rdd_attribute, id: :dashboard_rdd_attribute %>
  </div>

  <div class="field">
    <%= form.label :rdd_value %>
    <%= form.number_field :rdd_value, id: :dashboard_rdd_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
